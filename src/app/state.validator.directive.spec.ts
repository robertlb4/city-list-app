import { State.ValidatorDirective } from './state.validator.directive';

describe('State.ValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new State.ValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
